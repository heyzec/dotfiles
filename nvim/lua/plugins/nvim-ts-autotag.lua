-- Use treesitter to auto close and auto rename html tag
-- Treesitter parser for HTML is required: :TSInstall html
-- Note that luasnip also provides such common HTML snippets
return {
    "windwp/nvim-ts-autotag",
    opts = {},
}
