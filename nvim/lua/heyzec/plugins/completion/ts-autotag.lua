-- Automatically close HTML-style tags and renames HTML tags without LSP using Treesitter
-- (note that LuaSnip also can provide common HTML snippets)
return {
  'windwp/nvim-ts-autotag',
  opts = {},
}
