-- Automatically close HTML-style tags, and also
-- renames HTML tags without LSP using Treesitter
-- (Note that LuaSnip also can provide common HTML snippets)
return {
  'windwp/nvim-ts-autotag',
  opts = {},
}
