# define a custom 'delete' command
cmd delete ${{
    set -f
    printf "$fx\n"
    printf "delete? [y/n]"
    read ans
    [ "$ans" = "y" ] && rm -rf $fx
}}

# image previews in terminal
# see ~/.config/lf/previewer
set sixel true
set previewer ~/.config/lf/previewer

